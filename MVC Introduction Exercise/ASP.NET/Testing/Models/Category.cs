namespace Testing.Models
{
    public class Category//made a new category model with its own properties, so that it's identifiable as its own data table.
    {
        public int CategoryID { get; set; }
        public string Name { get; set; }
    }
}
